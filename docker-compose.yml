services:
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}

  # no-fap-api-gateway:
  #   build:
  #     context: .
  #     dockerfile: apps/no-fap-api-gateway/Dockerfile
  #   environment:
  #     SERVICE_PORT: 3000
  #   ports:
  #     - '3000:3000'

  # user-service:
  #   build:
  #     context: .
  #     dockerfile: apps/user-service/Dockerfile
  #   environment:
  #     SERVICE_PORT: 3002
  #   ports:
  #     - '3002:3002'
